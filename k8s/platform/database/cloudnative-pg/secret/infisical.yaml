# yaml-language-server: $schema=https://github.com/datreeio/CRDs-catalog/raw/refs/heads/main/external-secrets.io/externalsecret_v1.json
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: infisical-db-cred
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  finalizers:
    - external-secrets.io/finalizers
spec:
  refreshInterval: 12h
  secretStoreRef:
    name: aws-parameter-store
    kind: ClusterSecretStore
  target:
    name: infisical-db-cred
    template:
      engineVersion: v2
      mergePolicy: Replace
      data:
        username: infisical
        password: "{{ .password }}"
  data:
    - remoteRef:
        key: /home/infisical/postgresql/password
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
      secretKey: password
