---
apiVersion: pipelinesascode.tekton.dev/v1alpha1
kind: Repository
metadata:
  name: home
  namespace: gitea
spec:
  url: https://gitea.u74n.internal/utan/home
  git_provider:
    url: https://gitea.u74n.internal
    secret:
      name: "gitea-webhook-secret"
      key: "provider.token"
    webhook_secret:
      name: "gitea-webhook-secret"
      key: "webhook.secret"
