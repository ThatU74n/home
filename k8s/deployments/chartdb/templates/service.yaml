---
kind: Service
apiVersion: v1
metadata:
  name: "{{ .Release.Name }}"
spec:
  selector:
    app: "{{ .Release.Name }}"
  type: ClusterIP
  ports:
    - protocol: TCP
      targetPort: 80
      port: {{ .Values.service.port }}
      appProtocol: http
