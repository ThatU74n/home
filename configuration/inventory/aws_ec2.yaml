---
plugin: amazon.aws.aws_ec2
regions:
  - ap-southeast-1
filters:
  instance-state-name:
    - running

compose:
  ansible_host: public_ip_address
  node_name: tags.Name

keyed_groups:
  - key: tags.Role
    separator: ""
