---
- name: Reload apt cache
  become: true
  when: ansible_facts['os_family'] == 'Debian'
  ansible.builtin.apt:
    update_cache: true

- name: Reload dnf cache
  become: true
  when: ansible_facts['os_family'] == 'RedHat'
  ansible.builtin.dnf:
    update_cache: true
