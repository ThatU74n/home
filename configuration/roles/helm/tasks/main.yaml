---
- name: Install (Debian)
  when: ansible_facts['os_family'] == "Debian"
  ansible.builtin.import_tasks: install_debian.yaml

- name: Install (MacOS)
  when: ansible_facts['os_family'] == "Darwin"
  ansible.builtin.import_tasks: install_macos.yaml
