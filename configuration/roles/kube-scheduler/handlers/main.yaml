---
- name: Reload systemd
  become: true
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart Kube-scheduler
  become: true
  when: kube_scheduler_mode == "systemd"
  ansible.builtin.systemd:
    name: kube-scheduler
    state: restarted
