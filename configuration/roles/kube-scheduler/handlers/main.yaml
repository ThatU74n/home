---
- name: Reload systemd
  become: true
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart Kube-scheduler
  become: true
  when: _mode == "systemd"
  ansible.builtin.systemd:
    name: kube-scheduler
    state: restarted
